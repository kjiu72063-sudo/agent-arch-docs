# 理论精讲 · Track A 深化方案（从"会用级"到"设计者级"）

依据权威源：cnblogs loop / iceyao DeepSeek Harness / LangGraph Graph API / LangChain deepagents / deusyu harness-engineering / 腾讯云 Skills 技术体系。
状态：深化方案（尚未改稿）。目标：让每个概念章不只在"认识/会用"档，而是具备可运行证据、设计决策表、可验证验收标准。

---

## 0. 深化核心判断

前两轮审查已把 02–06 从"单页"抬到"子页 + 机制 + 可运行片段"。但它们仍处于**"一个机制点配一段说明"**的层级。权威源揭示的下一步是另外三种"深度"：

1. **机制深度**——机制之间有**触发条件、数据流、边界**（如 compaction 的 prune/summarize 分派、reducer 在多节点下的合并规则），而不是名词并列。
2. **工程证据深度**——给出**可运行代码 + 真实性能/数据点 + 决策表**（deusyu 给出"3 人→7 人、1500 PR、人均日 3.5 PR"；cnblogs 给出"测试 8→23、爬虫一次修复"）。
3. **方法论深度**——从"这是什么机制"到"何时选它 / 如何衡量 / 如何验证"（含反模式与代价）。

一句话：**当前是"机制手册"，目标是"设计决策 + 可运行系统"。**

---

## 1. 深化总纲：每章统一的"设计者级"交付模板

每一章（02 06）在现有 `mechanism / pitfalls` 之上，等级到：

| 层级 | 现状(v0.5) | 深化后应新增 |
|---|---|---|
| L1 概念 | ✅ | — |
| L2 机制 | ✅ | 补机制间的**触发/数据流/边界** |
| L3 可运行实现 | ✅ | 补**多机制协同的最小系统** + 集成门禁 |
| L4 设计决策 | ✖ | 补**何时选A不选B** 决策表 + 代价/反模式 |
| L5 验证 | ✖ | 补**验收脚本/指标阈值**（用 gate 脚本断言） |

---

## 2. 05 graph：最优先深化（差距最大）

**权威源关键事实**（LangGraph Graph API）：
- reducer 有三类：**Replace（默认）/ Append（`operator.add`）/ 自定义二元函数**；`Annotated[type, reducer]` 声明；并行节点输出**经 reducer 确定性合并、无竞态**。
- checkpoint 生产后端：`InMemorySaver`(仅测试)/`PostgresSaver`(生产推荐, 需 `autocommit`+`setup()`)/`DynamoDBSaver`(serverless)；`thread_id` 隔离状态，同 `thread_id` 续跑、不同则新跑。
- **Human-in-the-loop**：`interrupt_before/interrupt_after`(静态) 与 `interrupt()`(动态)；`interrupt` 可暂停数小时。
- 时间旅行：checkpoint 支持 replay/回放调试；`recursion_limit` 防止超步死循环。
- 并行/扇出：`Send`（动态 fan-out，每条目一次节点调用）；map-reduce。
- 子图(subgraph) ：graph 节点可以是另一个 graph。

**深化方案**：graph 章新增 `graph/design.md`（设计决策）并把 mechanism 升级为多机制协同系统：

1. **reducer 从"说明"变"对照实验"**：给出同一 State 下 Replace vs `operator.add` 的运行差异，含并行节点输出合并演示。
2. **checkpoint 从"内存"到"生产"**：给 `PostgresSaver` 完整接线（连接池 + `setup()` 放 CI 不在热路径），并讲 `thread_id` 隔离 → 是多会话续跑的关键（呼应 02 的记忆分层）。
3. **HITL 决策表**（新增 L4）：
   | 场景 | 用哪种 interrupt | 理由 |
   |---|---|---|
   | 审批节点前暂停 | `interrupt_before` | 确定位置 |
   | 运行时被规则触发暂停 | `interrupt()` | 条件未知 |
4. **Send 并行**（新增 mechanism）：给 `return [Send("worker", item) for item in items]` 扇形实现，配合 `operator.add` 归并——证明"reducer 使并行安全"。
5. **验收**（新增 L5）：一个 gate 脚本跑通「条件边 + reducer 累加 + checkpoint 续跑 + interrupt 恢复」，断言 4 项全通过。

**设计者级判定**：能在 LangGraph 里实现一个可中断、可续跑、可并行归并的生产图，并说出该用哪种 checkpointer 与 interrupt。

---

## 3. 03 harness：补"方法论 + 少数人运行的吞吐量体系"

**权威源关键事实**（deusyu/harness-engineering + OpenAI 原文）：
- 六大核心概念（比站点现有的 5 条更精确）：
  - 仓库即记录系统（不在仓库=对智能体不存在）；站点已讲，需补"决策/规范/计划都以版本化工件提交"。
  - **地图而非手册**——`AGENTS.md` 是**约 100 行的目录页，不是百科全书**；巨型指令三死因：挤占上下文/无法维护/无法机械验证；站点的 AGENTS.md 样例写得像手册（过长），需要反过来重新示范"目录 + 渐进披露"。
  - **机械化执行**——文档会腐烂，lint 规则不会；**lint 错误信息内嵌修复指令**，智能体可自我纠正；中央强制边界、本地允许自主。站点缺"错误信息可被 agent 读取并自纠"这一环。
  - **熵管理 = 垃圾回收**——技术债是高息贷款；智能体会复现仓库已有模式（含坏模式）；"黄金规则"编码进仓库 + 后台任务扫描偏差 + 更新质量评分 + 发重构 PR。站点只讲了"压缩/归档"，缺"质量评分 + 后台扫描"。
  - **吞吐量改变合并理念**——纠错成本低、等待成本高；测试偶发失败靠重跑解决。这是 harness 在多人 agent 体系里的组织结论，站点完全没有。
  - 智能体可读性——选"无聊"稳定技术；可 worktree 启动。
- **Ralph 循环**（harness 实战框架）：六信条 Fresh Context / Backpressure / Plan Is Disposable / Disk Is State Git Is Memory / Steer With Signals / Let Ralph。与站点 loop 章直接相关。
- **C1–C14 十四层一致性校验**：deusyu 用 `scripts/check-consistency.sh` 守"计数不漂移"，C14 派生侧栏、拒 symlink。这是"机械化执行"最完整的落地示范——站点可比照建一个简化版 gate。

**深化方案**：
1. harness 机制新增 **"地图而非手册"正确示范**：把现有长 AGENTS.md 改成 ~100 行目录 + 指向深层文档，并列对比"手册式 vs 地图式"。
2. 机械化执行新增 **"agent 可自纠"反馈环**：lint/门禁不仅 PASS/FAIL，输出含修复指令，agent 读报错自行修正（把 01 的"把失败信息喂回"与 04 的 Prompter 反馈闭环起来）。
3. 熵管理升级为 **"质量评分 + 后台扫描"模型**：给"黄金规则 → 扫描偏差 → 质量分 → 重构 PR"流程与决策表，并给出简化 gate 数据流。
4. **吞吐量/合并理念**（新增 L4）：给"何时重跑 vs 等待"决策表（测试偶发失败=重跑；确定性失败=人工），这是多人 agent 体系才需要的结论。
5. **验收**：gate 脚本断言 AGENTS.md ≤ 某阈值行数 + 门禁输出含修复指令 + 熵扫描能发现一处故意注入的坏模式。

**设计者级判定**：能为一个 3–7 人 agent 团队搭 harness，含目录式 AGENTS.md、agent 可自纠的门禁、后台熵扫描，并解释吞吐量下的合并策略。

---

## 4. 04 loop：补"Open/Closed 选型 + Maker-Checker 工程 + 实战证据"

**权威源关键事实**（cnblogs Loop Engineering 完全指南）：
- Open Loop 灵活/成本不可预测；Closed Loop 稳定/不灵活；**实践：先 Open 探路，固化 Closed 上生产**（站点机制页只一句带过）。
- Maker-Checker 分离是核心工程原则（站点机制已讲 Verifier 独立，但**没给两个实战闭环**）。
- 三刹车伪代码（站点已转 Python），但缺真实数据点与批量修复案例。
- **Stop condition 是"布尔表达式"**，prompter 反馈式重组（站点 mechanism 已加，缺联动闭环演示）。

**深化方案**：
1. **Open/Closed 决策表**（新增 L4）：场景 → 形态 → 成本/灵活性权衡。
2. **Maker-Checker 双案例**（新增 mechanism 下半）：举 cnblogs 两个案例（夜间自动修 bug = writer/reviewer 分离 + git worktree 并行；web scraping = rubric + 独立 checker，首次尝试修复即达标），把"独立评估器用便宜模型"落到代码。
3. **验证升级为"可运行闭环"**：给一个最小 `loop_with_brakes.py` 之上加 `Maker` 与 `Checker` 两个函数 + 一个 goal 布尔表达式，断言"不达标不退出、带刹车必停"。
4. **Ralph 信条映射**（新增设计页）：Fresh Context↔智能体可读性、Backpressure↔机械化执行、Plan Is Disposable↔熵管理、Disk Is State/Git Is Memory↔仓库即记录，串起 harness 与 loop。

**设计者级判定**：能写一个 Open/Closed 可切换、Maker/Checker 分离、带三刹车的循环，并给出为何选该形态的论证。

---

## 5. 02 context：补"compaction 的真正实现链路 + DSE"

**权威源关键事实**（iceyao DeepSeek Harness + LangGraph/Anthropic）：
- **compaction 分 `pressure`(token 超阈值) 与 `context-overflow`(服务端报错) 双触发**（站点已讲），但深层：
  - **prune(免费删减) → summarize(LLM 摘要) 五步流水线**，且压缩"不绕过日志"——用 `surfaceOp: replace` 追加事件让投影收缩，保持可审计+可收缩。
  - **DSE（确定性信号提取）**：从原始对话提取结构化信号（实体/意图）而非只做 LLM 摘要——比通用摘要更省、更可复现。
- 上下文同步与远程隔离机制。

**深化方案**：
1. compaction 升级为**五步流水线图 + prune/summarize 分派决策表**（何时只 prune、何时动用 LLM 摘要）。
2. 补 **DSE 作为替代/补充**：给"确定性提取 vs LLM 摘要"对照与决策表（哪些信息该确定性提取、哪些可摘要）。
3. **预算计量**：说明用真实 tokenizer 而非 `len(split())`，给出估算误差与原因（站点 Python 骨架用了粗略估算，需在方案注明生产用表驱动精确计数）。
4. 验收：预算控制器在超预算输入下降配不崩（已有）；新增由 `side-by-side` 记录物（append-only 日志 + surface 投影）演示"压缩不丢可审计性"。

**设计者级判定**：能实现 pressure/overflow 双触发 + prune/summarize 分派 + DSE 的上下文管线，且保持事件可回放。

---

## 6. 06 skill：补"统一接口规范 + 生命周期 + 三层代码模式"

**权威源关键事实**（腾讯云 Skills 技术体系）：
- 四层架构（站点已画）；但权威源给出**统一接口规范**（`id/name/description/version/inputs/outputs/execute/metadata`）、**六阶段生命周期**（需求→设计→测试→发布→监控→迭代）。
- 三种模式有**真实代码**：组合（DataAnalysisSkill 编排子 skill）、策略（按类型选 JsonProcessor/Xml/Csv）、装饰器（LoggingSkillDecorator 加日志/缓存）——站点的装饰器是 Python 泛例，权威源给了"给 skill 加日志/缓存"的业务化版本。
- 状态管理、并发、缓存（`Promise.all`、`Map` 缓存、资源回收）——站点无。

**深化方案**：
1. mechanism 新增 **统一接口规范**（TypeScript `interface Skill`），作为"可交付 skill"的骨架。
2. **生命周期六阶段**（新增设计页），明确每阶段产出与验证。
3. 三种模式给**与 skill 业务对齐的代码**（组合=一个业务 skill 编排子 skill；策略=按格式分派；装饰器=加日志/缓存/鉴权横切），替换/补足泛例。
4. **性能与资源**：并发（Promise.all）、缓存、资源释放 三小节（新增 mechanism 尾部）。
5. 验收：给一个"被 discovery 命中 → 执行 → 日志+缓存装饰生效"的 gate。

**设计者级判定**：能按统一接口交付一个含生命周期、组合/策略/装饰器、缓存/并发/资源管理的可复用 skill。

---

## 7. 实例章联动（Track B）

DeepAgent 权威源（LangChain deepagents）为 `deepagent` 章补深度：
- 中间件序列：`ContextSummarizationMiddleware`(超 30 消息压缩) / `ContextExtractionMiddleware` / `SubAgentMiddleware` / `AsyncToolsMiddleware`。
- 三个 toolkit（fs / interactive bash / safe code interpreter）。
- `ictxt`-style 手动上下文工具 `add_context/delete_context`。
- 状态 channel：`messages(add_messages)`、`intermediate_steps(operator.add)`、`context_manager(自定义 reducer)`——**证明 05 graph 的 reducer 在真实框架里被用到了哪些键**。

深化动作：`instances/deepagent.md` 补"中间件时序图 + 三个 toolkit 表格 + add_context 工具"，并把 reducer 键与 05 章互链。

---

## 8. 优先级与验收

| 优先级 | 章 | 动作 | 验收 gate |
|---|---|---|---|
| P0 | 05 graph | 机制升级 + 新增 design（HITL/Send/reducer 对照/生产 checkpointer） | 一脚本跑通条件边+reducer+checkpoint+interrupt |
| P0 | 03 harness | 地图式 AGENTS.md + 自纠门禁 + 熵扫描 + 吞吐量决策表 | gate 断言 AGENTS≤阈值/门禁含修复指令/熵扫描命中坏模式 |
| P1 | 04 loop | 决策表 + Maker-Checker 双案例 + 可运行闭环 + Ralph 映射 | 带刹车+分离 Verifier 的循环满意才退出 |
| P1 | 02 context | compaction 五步 + DSE + 预算计量 + 事件可审计演示 | 双触发不缺事件、压缩不丢审计性 |
| P1 | 06 skill | 接口规范 + 生命周期 + 业务化三模式 + 并发/缓存/资源 | 命中→执行→装饰生效 via gate |
| P2 | Track B | deepagent 中间件/toolkit/context 工具补充 | 与新机制页互链可达 |

**总验收**：每章一个 `gate` 脚本 + 一个设计决策表；全站 45 页之上新增约 6–8 页（2–6 各 +1 design 或扩 mechanism；实例 +1）；build exit 0，全部 200。

---

## 9. 交付形态建议

- **不改当前 mechanism/pitfalls 结构**，在每章下**追加 `design` 页**或把 mechanism 扩成"多机制协同系统"，避免推翻已通过 v0.5。
- 决策表用 Mermaid `flowchart` 或 Markdown 表格；可运行证据用可跑 Python/TS 片段 + 注释结果。
- 全站加一个 `gate` 约定：每章 L5 验收脚本，放 `concepts/<xx>/gate/`，能本地 `python gate.py` 断言。

---

*本方案以六个权威源的真实机制/数据点为锚，定位为"从会用级到设计者级"的下一步。改稿动作待授权后按 P0→P1→P2 执行。*