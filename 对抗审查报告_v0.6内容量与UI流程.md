# 理论精讲 · v0.6 三阶段对抗审查（内容量 + UI 流程顺序）

审查对象：`agent-arch-docs` v0.6（Track A 六章各 3 子页 + gate；Track B 六实例；Track C 四实战）
审查维度：内容量（量化）· 衔接 · 难度过渡 · **UI 流程顺序** · 验证真实性
审查方式：反方论证 → 支持方（客观成立条件）→ 中立裁判
日期：2026-09-16

> 前置纠错：上一轮我报告"5 个 gate 全部实测 PASS(4/4)"，该陈述**具有误导性**——实测为真，但 4/5 个 gate 是模拟自证（见炮点 2），其"通过"不等于验证了任何真实系统。本轮据实修正。

---

## 阶段一 · 反方论证（最强反方立场）

### 核心判断
v0.6 只完成了**结构升级**（design 页 + gate 脚本），**未完成实质升级**。站点呈现出"框架完整、内容稀薄、验证自证"的状态——**看起来完成度高，实际仍停在表层**。再补页面只会加剧稀释。

### 炮点 1 · 内容量与权威源差距悬殊（量化铁证）

| 对象 | 站点体量 | 权威源体量 | 差距 |
|---|---|---|---|
| 04 loop 全章（3 子页） | **10,174 字符** | cnblogs《Loop Engineering 完全指南》单篇 ≈15,000+ 字符 | ≈2/3，且站点含图表/测验占位，实质知识更少 |
| deepseek-harness 实例 | **2,513 字符** | iceyao 源码深度解析 ≈15,000+ 字符 | **≈1/6** |
| claude-code 实例 | **2,204 字符** | sawzhang《deep-dive-claude-code》25 章 | 数量级差距 |
| 03 harness 全章 | **8,939 字符** | deusyu 仓库**仅 README** 即 20,364 字符 | **≈1/2**（还只是人家的 README） |
| 六实例平均 | **≈90 行 / 2.2k 字符** | 各自权威源码解析动辄上万字 | 普遍 1/5 以下 |

**结论**：站点一个"章"的体量 ≈ 权威源一篇文章的 1/2～2/3；一个"实例"≈ 1/6。所谓"知识体系"，在内容量上只相当于权威源的**目录级摘要**。

### 炮点 2 · gate 是自证式伪验证（最致命）

对 5 个 gate 脚本的真实性核查：

| gate | 模拟/示意标记数 | 是否真实调用框架 | 判定 |
|---|---|---|---|
| `graph_gate.py` | 0 | **真 `from langgraph`** | ✅ 唯一真实验证 |
| `loop_gate.py` | **11** | 用 `FakeMaker`/`FakeChecker` | ❌ 自证模拟 |
| `context_gate.py` | 4 | token 数 hardcode `realistic = 9` | ❌ 自证模拟 |
| `skill_gate.py` | 2 | Python 模拟 `Promise.all`/装饰器 | ❌ 自证模拟 |
| `harness_gate.py` | 5 | 检查内置字符串常量 `SAMPLE_MAP_STYLE` | ❌ 自证模拟 |

**它们"自己构造输入、自己断言输出"，必然 PASS——等于什么都没验证。** 上一轮我把这称为"L5 可运行验证"是名不副实的：4/5 个 gate 只是"演示脚本"，却被当作验收证据，这比没有 gate 更危险（制造虚假信心）。

### 炮点 3 · 实例章仍是标签化，权威源未被消化

用户提供的 5 个深度权威源（Hermes 源码研读、sawzhang deep-dive 25 章、Codex 剖析、iceyao 源码解析、deepagent csdn），实际消化情况：

- **仅 deepagent** 补了中间件表 / 三 toolkit / `add_context` / 状态 channel（全站唯一达到"源码级"的实例章）；
- **其余五章**（Hermes / Claude Code / Codex / OpenClaw / DeepSeek Harness）仍是"映射图 + 名词 + 三档自检"三段式，**无一处真实源码或配置**。
- 例：Claude Code 章以"六层权限"为核心卖点，却只有一张表，没有真实 `CLAUDE.md`、没有 `settings.json` 权限配置、没有 hook 示例——**最有价值的部分恰恰是空的**。

### 炮点 4 · UI 流程顺序倒置（本次新增维度）

当前每章子页顺序：

```
mechanism(2-1 机制) → pitfalls(2-2 坑+自检) → design(2-3 设计决策)
```

**问题**：`pitfalls` 讲"这样会踩什么坑"，但读者此时**还没学 design 的设计决策**——先讲坑、后讲如何设计，**顺序倒置**。
且章入口"本章地图"表与 `config.mjs` 侧栏都按此错误顺序固化，读者被流程引导着"先背坑、后学设计"。

**正确顺序**应为：`mechanism`（机制）→ `design`（如何设计与决策）→ `pitfalls`（这样设计会踩什么坑 + 自检）。

### 炮点 5 · 难度过渡仍无梯度

全站**无任何难度标记**（无 L1–L5、无入门/进阶/高级）。Track 0（零基础"什么是 LLM"）→ Track A 第一章 prompt 仍是断层；每章 3 子页之间也无难度标识，读者无法判断 `design` 是最高阶内容。

### 炮点 6 · 衔接仍停留在"文字声明"

虽加了"承接关系"段，但未形成**数据流级**衔接：context 预算如何随 loop 多轮消耗、harness 的 AGENTS.md 何时注入、graph 节点如何消费 skill——这些仍是并列声明，读者仍拼不出一条可追踪的数据流。

---

## 阶段二 · 支持方（客观成立条件，非吹捧）

1. **结构完整**：12 知识点全覆盖，每章 mechanism/pitfalls/design 三子页 + gate，模板统一，导航一致。
2. **graph_gate 是真实可运行验证**：真 `langgraph` 跑通条件边 / reducer 累加 / checkpoint 续跑 / Send 并行归并四项——这是全站唯一真实 L5，但确实成立。
3. **权威源选择方向正确**：所引 10 个源均为该领域高质量来源；【事实】/【推断】/【建议】三级标注机制真实且贯穿一致。
4. **工程链路可用**：`npm run build` exit 0（52 页）、全部页面 HTTP 200、已部署 GitHub 可复现。
5. **相对 v0.4 是实质进步**：每章有决策表与产物化自检，不是原地踏步。
6. **deepagent 章达到源码级**：中间件表 / 三 toolkit / `add_context` / 状态 channel，与 05 reducer 互链——为其余实例章提供了可复制范本。

---

## 阶段三 · 中立裁判：此刻如何修改

**总判**：**停止增加页面数量**。当前病灶是"结构膨胀、实质未增"，再加页只会稀释。要做三件事：**假验证换真验证、标签实例换源码级、UI 顺序调正**。

### P0（必须，否则"验证"是假的）

1. **处理 4 个模拟 gate**（二选一，不能继续含糊）：
   - **升级为真验证**：`loop_gate` 调用真实 agent 框架跑通；`context_gate` 用 `tiktoken` 真实计数（删除 `realistic = 9`）；`skill_gate`/`harness_gate` 改为对真实 skill 文件/配置断言。
   - **或降级标注**：重命名为 `*_demo.py`，并在文档明确写"概念演示，非验收"。
   - **禁止再声称"5 个 gate 全部通过"**——当前只有 graph 是真验收。
2. **实例章源码级化**：每章至少 1 段真实源码/配置 + 逐行解读。优先级：
   - Claude Code：真实 `CLAUDE.md` + `settings.json` 权限 + hook 示例（消化 sawzhang）；
   - DeepSeek Harness：iceyao 的 `ReactLoopAgent` / 事件溯源片段；
   - Hermes：工具注册代码片段（消化 luyao618）。

### P1（UI 流程顺序 + 难度）

3. **调整子页顺序**：`mechanism → design → pitfalls`（机制 → 设计决策 → 坑与自检）。同步修改：各章入口"本章地图"表顺序、`config.mjs` 侧栏 items 顺序、各页底部 prev/next 链接。
4. **加显式难度标记**：每页标注 L1–L5 或 入门/进阶/高级；在 Track 0 与 Track A 之间补过渡说明，消除断层。

### P2（内容量）

5. **扩充 mechanism**：把权威源完整案例写进去——loop 章补 cnblogs 三个完整案例（含 git worktree 并行修复、web scraping rubric 闭环）；context 章补真实 tokenizer 与完整 compaction 实现。
6. **诚实定位**：首页/README 明确"概念地图 + 权威源导航 + 部分可运行验证"，在内容量追上权威源之前**不要自封"知识体系"**。

---

*本报告基于对 5 个 gate 脚本、各章体量统计与权威源规模的实测比对得出，包含对本人上一轮"5 个 gate 全部 PASS"陈述的纠错。无主观吹捧。*