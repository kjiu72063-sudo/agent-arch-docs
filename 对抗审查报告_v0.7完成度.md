# 理论精讲 · v0.7 三阶段对抗审查（内容量 / 衔接 / UI 顺序 / 难度）

审查对象：`agent-arch-docs` v0.7（Track A 六章三子页 + gate；Track B 六实例；Track C 四实战）
审查方式：量化实测（字符数 / 代码块语言分布 / 顺序核验）→ 反方 → 支持方 → 中立裁判
日期：2026-09-17

---

## 阶段一 · 反方论证（最强反方立场）

### 核心判断
v0.7 修好了**结构性缺陷**（UI 顺序、难度标记、定位诚实），但**内容量层面的"半成品"暴露得更清楚**：整改是"点状完成"而非"面上达标"——**同类章节里，一半达标、一半仍空**。这不是"深度不够"，而是**完成度不均**。

### 炮点 1 · 实例章"源码级化"只做了一半（铁证）

对 Track B 代码块语言分布的实测：

| 实例章 | 字符数 | 语言分布 | 判定 |
|---|---|---|---|
| claude-code | 3582 | `json×2 + markdown×1 + mermaid×2` | ✅ 有真实配置 |
| deepseek-harness | 4466 | `ts×2 + mermaid×3` | ✅ 有真实源码 |
| hermes | 3490 | `python×2 + mermaid×2` | ✅ 有真实源码 |
| **codex** | **1981** | **`mermaid×2`（仅图）** | ❌ **零真实代码** |
| **openclaw** | **1894** | **`mermaid×2`（仅图）** | ❌ **零真实代码** |
| **deepagent** | **3265** | **`mermaid×2`（仅图）** | ❌ **零真实代码** |

**6 个实例里 3 个仍只有"映射图 + 名词 + 自检"三段式**，与 v0.5 批评的"贴标签"状态**完全一致、未改**。而你提供的权威源（smartloli 的 Codex 剖析、openclaw-docs）恰好覆盖这两章——**给了源却没消化**。deepagent 更讽刺：它自己标榜"中间件机制"，却连一行中间件代码都没有。

### 炮点 2 · 内容量分布严重不均（不是"都够"，是"有的够有的空"）

Track A 各章字符数：
```
loop 13669 · context 13150 · graph 10607 · harness 9519 · skill 9169 · prompt 6555
```
- **最大反讽**：`prompt`（6555）是 Track A **最薄**的一章——而它恰恰是被拿来当"对标基准"去批评 02–06 的那一章。它 7 个子页平均仅 **~780 字符/页**（`ref-split` 只有 397 字符），**比它批评的对象更薄**。
- 实例章极差 2.4 倍（deepseek 4466 vs openclaw 1894）。同一个 Track 里，读者体验断裂。

### 炮点 3 · pitfalls 页是"凑数页"

| 页 | 字符数 |
|---|---|
| 各章 pitfalls | 1100–1265（context 1199 / harness 1151 / loop 1113 / graph 1265 / skill 1191）|

它们基本只有"4 条坑 + 一张三档表"，**内容密度全站最低**。它们的存在是为了**满足"三子页结构"**，而不是因为有内容要讲——这是为结构而结构。

### 炮点 4 · gate 是"隐形资产"（顺序修复后暴露的新问题）

把 design 页提前到 x-2 后，design 页**大量引用 gate 脚本**，但：
- gate 在文件系统里是 `concepts/x/gate/*.py`，**读者在页面上根本看不到**；
- 六章里**只有 graph 建了 `gate/graph_gate.md` 页面**，其余 4 章（context/harness/loop/skill）的 gate **没有页面**，只被正文文字提及。

结果：读者读到"运行 gate 验收"，点不进去、看不到脚本。**gate 成了只有作者知道的资产**。

### 炮点 5 · 难度标记是"标签"不是"梯度"

6 章入口各加了 1 行难度标注（`grep -c 难度` 全为 1），但：
- **子页内部无难度设计**——mechanism 页里没有"从 L0 到 L1"的内容分层，design 页直接上 L2；
- Track 0→A 的 L0/L1/L2 阶梯表是**阅读建议**，不是**内容调节**（子页本身没按难度重排内容）。
标注了难度，但内容没有随难度变化——**标签与实质脱节**。

### 炮点 6 · 衔接仍是"文字声明"

章与章之间（02↔03↔04↔05↔06）的"承接关系"仍是文字段落；Track B 实例与 Track A 概念的关联是"相关概念：xxx"列表，**不是"本实例用到 05 的 reducer 在 messages 键上"这种精确机制映射**（deepagent 章做到了这一处，其余没有）。读者仍需自己拼数据流。

---

## 阶段二 · 支持方（客观成立条件，非吹捧）

1. **UI 顺序已真正修复**：`mechanism → design → pitfalls` 三处一致（章入口地图表 / `config.mjs` 侧栏 / 各子页 prev-next），实测侧栏顺序正确。
2. **难度标记已落地**：6 章入口均有 L0–L2 标注；Track 0 加了 L0/L1/L2 阶梯表。
3. **gate 全部真实通过**：graph（真 LangGraph）/ context（真 tiktoken）/ harness、skill（真实文件断言）/ loop（真控制流），**无自证模拟**。
4. **两章内容量已接近权威源**：loop 13669、context 13150 字符（cnblogs 原文 ~15000），达到原文 85%+ 量级。
5. **3 个实例章达源码级**：claude-code（CLAUDE.md/settings.json/hook）、deepseek（ReactLoopAgent/事件溯源）、hermes（工具注册）——为其余章节提供了可复制范本。
6. **定位诚实**：首页与 README 明确声明"概念地图 + 权威源导航 + 部分可运行验证"及 gate 验证边界。
7. **工程可复现**：build exit 0（53 页）、全部页面 HTTP 200、已推 GitHub。

---

## 阶段三 · 中立裁判：此刻如何修改

**总判**：v0.7 的问题**不是"深度不够"，而是"完成度不均"**。整改呈点状——同类章节一半达标一半空。**下一步应"补齐短板"而非"继续增加新层"**。

### P0（补完"半成品"，否则 Track 内体验断裂）

1. **补齐 3 个实例章的真实代码**（最高优先，因为用户已提供权威源）：
   - `codex`：补 `AGENTS.md` + `config.toml`/sandbox 配置 + 执行循环片段（消化 smartloli 剖析）；
   - `openclaw`：补 Gateway 配置 + 一个通道连接器/插件骨架（消化 openclaw-docs）；
   - `deepagent`：补 `create_deep_agent` 调用 + 中间件配置 + 子 agent 定义代码（消化 LangChain deepagents）。
2. **补齐 4 章的 gate 页面**：为 context/harness/loop/skill 各建 `gate/*_gate.md`（对齐 graph 已有），让 gate 从"隐形"变"可见可点"。

### P1（拉平内容量与密度）

3. **prompt 章扩容或重构**：7 个子页共 6555 字符、平均 780 字符/页——**比它批评的 02–06 更薄**。要么给关键子页（basics/clarity/think-tools-test）扩容到 ~2000 字符，要么合并弱页（ref-split 仅 397 字符）。
4. **pitfalls 页要么加厚、要么合并**：1100–1265 字符的"4 坑 + 1 表"密度最低。建议把 pitfalls 并入 design 页（合成"设计决策 + 坑"），或每页补真实反模式案例。
5. **均衡实例章**：openclaw/codex 向 deepseek（4466）看齐，目标 ≥3500 字符 + 真实代码。

### P2（让标注与实质一致）

6. **难度从"标签"变"梯度"**：子页内部按难度分层（如 mechanism 页分"L0 认识 / L1 会用"两段），而不是只在章入口标一句。
7. **衔接从"文字"变"映射表"**：每个实例章加一张"本实例 × 主轴机制"的**精确映射表**（如"OpenClaw 的多通道会话存储 ← 02 context 记忆分层"），替代泛泛的"相关概念"。

### 验收口径建议
- 每个 Track B 实例章：**≥1 段真实代码/配置 + 字符数 ≥3500**；
- 每章 gate：**有独立页面可达**；
- 每章 pitfalls：**≥1500 字符或已合并**；
- 全站章节字符数**极差 ≤1.8 倍**（当前 2.4 倍）。

---

*本报告基于对 6 个实例章代码块语言分布、各章字符数、侧栏顺序与难度标记的实测得出。承认 v0.7 在 UI 顺序与定位上的真实修复；指出其"点状完成"的完成度不均问题。无主观吹捧。*