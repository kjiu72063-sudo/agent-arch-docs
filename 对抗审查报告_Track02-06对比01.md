# 理论精讲 · Track A 02–06 对抗审查（以 Track 01 prompt 为基准）

审查对象：`concepts/{context,harness,loop,graph,skill}.md`（02–06，各单页）
参照基准：`concepts/prompt/`（Track 01，7 子页：intro/basics/clarity/ref-split/think-tools-test/instances/pitfalls）
审查方式：逐章以 01 的结构与深度为准绳比对，反方 → 支持方 → 中立裁判。
日期：2026-09-16

---

## 阶段一 · 反方论证

### 核心对比结论

| 维度 | Track 01 prompt（7 子页） | Track 02–06（各 1 单页） |
|---|---|---|
| 深度分摊 | 7 页分摊一整套知识 | 每章一页塞进一整章 |
| 烂 vs 好 | 逐条对照（intro/clarity） | 无（只有抽象类比） |
| 实操技巧 | 角色/分隔标签/单任务（clarity） | 几乎无给读者的可操作步骤 |
| 实例落地 | 真实框架对照表（instances） | 实例章停留在"贴标签" |
| 常见坑 | 4 条独立坑清单（pitfalls） | 02–06 无任何"坑"章节 |
| 三档能力 | "描述到产物"（能判断失败根源/模板即产品） | "承诺能力"（能实现/能选型），无材料支撑 |

**一句话**：01 是"一套可学会的 engineering"，02–06 退化成"名词思维导图"。用户在 01 建立的学习期待，在 02–06 被降档兑现——**前 1/6 与后 5/6 是两种质量水平**。

### 逐章炮点

#### 02 context（单页）
- 同时抛出分层注入/渐进披露/压缩/记忆分层 4 个策略，每个仅 1–2 句。
- **越界 + 重复**：渐进披露、`AGENTS.md` 注入已在 01 的 `basics/1-2` 讲透，02 原样复述、未新增深度；真正的增量（token 预算如何分配、RAG 召回 top-k 机制、compaction 的 pressure/overflow 触发条件）全部缺失。
- 无一张预算分配的实际算例，无 compaction 触发逻辑。

#### 03 harness（单页）
- 列 AGENTS.md/权限/机械化执行/熵管理/仓库即记录 5 个机制，每个仅名词级。
- 缺：真实 `AGENTS.md` 样例、权限 allow/deny 表、机械化执行的落地门禁（lint/测试伪代码）、fail-closed 原则（DeepSeek Harness 的关键设计，权威源在手却未用）。
- 01 的 `think-tools-test/1-5` 已将"机械化执行守护不变量"作为落点引向 harness，03 却**没有接住这个前向引用**——链断。

#### 04 loop（单页，已逐行比对 cnblogs）
- 缺 Verifier 独立评估原则（Generator 不给自家打分）、Trigger 四类表、三刹车伪代码、Open/Closed 两种形态。
- **衔接断裂反证**：01 的 `think-tools-test/1-5` 已埋"先推理后动手→loop 雏形"的落点，04 未回引，读者从前页获得的期待落空。

#### 05 graph（单页）
- 仅 5 个名词（Node/Edge/ConditionalEdge/State/Reducer），无一行业可运行代码。
- reducer 被错讲为"覆盖/追加/取最大"（实为 Channel/`operator.add` 语义）；缺 checkpoint/persistence。

#### 06 skill（单页）
- 四层 + 三模式（组合/策略/装饰器）定位清晰，但落地全空。
- 缺 `SKILL.md` 的真实结构样例、发现/注入的具体机制（向量/关键词匹配如何做）、版本的评审流程如何跑。

### 链上硬伤（对比 01 才暴露的最强证据）
- **`pitfalls/1-7` 末尾至今写着"下一章 context engineering（待编写）"**——这是 02–06 补全前遗留的废弃文案，从未更新。
- 铁证：**01 与 02–06 是两批生产、从未做链上回归**。读者点下一章时，前页脚注仍在宣称后文"待编写"。

---

## 阶段二 · 支持方（客观成立条件，非吹捧）
1. 01 与 02–06 的模板（一句话概念 / 类比 / Mermaid / 测验 / 三档表 / 上一页下一页）**完全统一**，读者切换时认知负担小——这是可验证事实。
2. 01 的子页化已证明"单章多子页"模式可行，给 02–06 提供了现成的扩建蓝本，路径明确。
3. 02–06 的【事实】标注**真实指向**了权威源（cnblogs/LangGraph/iceyao/deusyu/腾讯云），是"选对了源、没用足源"，而非选错。
4. 02 的"分层注入/渐进披露"与 01 概念一致，**无矛盾**（虽有重复）。
5. 06"横切支柱"的定位与 01"落点通向 harness"的叙事**不冲突**，主线自洽。

---

## 阶段三 · 中立裁判：怎么改

**根因**：不是"02–06 写得差"，而是"用单页的体量去装一整章 engineering"，注定只能名词化。**解法不是改文案，是把 02–06 从单页升级为子页体系，对齐 01 的 7 页结构。**

### P0（结构性，根除摊薄）
每章拆为 **4 个子页**，对齐 01 模板：
1. `xx-intro`：一句话概念 + 类比 + 烂 vs 好逐条对照；
2. `xx-mechanism`：机制详解，**含至少一段可运行片段**——loop 给三刹车 Python 骨架、graph 给 StateGraph 完整可运行代码、harness 给 AGENTS.md+权限表样例、context 给预算分配算例+compaction 触发、skill 给 SKILL.md 结构样例；
3. `xx-instance`：真实框架落地对照表（对齐 01/instances，用 iceyao/sawzhang/smartloli 源码）；
4. `xx-pitfalls`：坑清单 + 三档能力（描述到产物）。

### P1（链上回归，立刻做）
- 删除 `pitfalls/1-7` 末尾的"（待编写）"废弃铭文，改为指向 context 的正式链接。
- 每章补"承接 01 / 输出给下一章"桥段：context 接住 basics 的渐进披露并讲**增量**；harness 接住 think-tools-test 的机械化执行落点；loop 回引"先推理后动手"。

### P2（可验证性）
- 02–06 的三档"精通"档对标 01 写法，改为**产物化描述**：如 loop 精通 = "提交一段含三刹车的可运行循环脚本"，graph 精通 = "提交一个含条件边的 3 节点 StateGraph 并跑通"，而非空洞的"能实现/能选型"。

### P3（06 专项）
- 补 SKILL.md 真实结构样例、发现/注入具体机制、版本评审可操作流程。

---

*对比结论：02–06 相对 01 存在系统性深度降档与链上脱节，需子页化 + 链回归 + 能力产物化三管齐下。*