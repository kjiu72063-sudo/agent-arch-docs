# 理论精讲 · v0.8 三阶段对抗审查（P0/P1/P2 之后）

审查对象：`agent-arch-docs`（P0 补实例代码+gate 页 → P1 拉平内容量 → P2 难度梯度+映射表 之后）
审查方式：脚本取证（体量 / 代码块构成 / Mermaid 引用完整性 / 模板重复度 / 权威源 URL 覆盖率）→ 反方 → 支持方 → 中立裁判
日期：2026-09-17

---

## 阶段一 · 反方论证（最强反方立场）

### 核心判断
v0.8 把**表层指标**（字符数、gate 页存在、难度标签、映射表）做上去了，但暴露出更本质的问题：**引用体系悬空、最薄的一层没被碰过、增量靠模板复制**。一句话——**上一轮修的是"看起来薄"，这一轮暴露出"底子虚"**。

### 炮点 1 ·【一级缺陷】"事实源"页根本不存在——全书引用体系悬空

正文多处写"详见 [事实源](/practice/compare)"，但实测：

```
practice/compare.md 中的 URL 数量 = 0        ← 它不是事实源页，是"统一对比矩阵"
```

即：站点声称的"**事实源索引页**"**不存在**，读者被引导到一个**不含任何来源链接**的页面。
更荒谬的是，`compare.md` 自己写着"定稿前应在**事实源页**逐格复核"——**而那个页面不存在，复核没有落脚点**。这不是笔误，是**引用体系的结构性悬空**。

### 炮点 2 ·【一级缺陷】用户给的权威源，3 个完全没落地，且全站无链接

对用户提供的 10 个权威源做全仓 grep：

| 权威源 | 出现文件数 | 判定 |
|---|---|---|
| `cnblogs.com/xiaobaiysf`（**loop 权威源**） | **0** | ❌ **完全未落地** |
| `ChesterXue`（deepseek harness csdn） | **0** | ❌ **完全未落地** |
| `weixin_44733966`（deepagent csdn） | **0** | ❌ **完全未落地** |
| `cloud.tencent.com/.../2646885`（skill） | 1 | ⚠️ 仅 1 处，无集中索引 |
| `Hermes-Source-Code-Study` | 1 | ⚠️ 同上 |
| `deep-dive-claude-code` | 3 | ⚠️ |
| `smartloli`（Codex） | 3 | ⚠️ |
| `openclaw-docs` | 4 | ⚠️ |
| `deusyu/harness-engineering` | 4 | ⚠️ |
| `iceyao`（deepseek 源码解析） | 10 | ✅ 唯一较充分 |

**结论**：所谓"每章标注【事实】出处"，实际只是**"文字提到来源名"**，不是可溯源链接。loop 章的三个实战案例只写"出自 cnblogs《Loop Engineering 完全指南》"——**没有 URL**，读者无法核验；deepseek/deepagent 两章的核心 csdn 源**根本没进站**。

### 炮点 3 ·【体量失衡新暴露】Track C 成了新的最短板

| Track | 字符数 | 占比 |
|---|---|---|
| Track A（概念主轴） | 90189 | — |
| Track B（实例专项） | 25945 | — |
| **Track C（复刻实战）** | **7808** | **Track A 的 1/11.5** |
| Track 0（新手前置） | 11096 | — |

- Track C 四页平均仅 **1952 字符/页**（selfcheck 1502 / compare 1811 / path 1901 / build 2594）。
- 而首页 feature 明写"**Track C · 复刻实战：7 维对比矩阵、学习路径、自研 harness**"——**"自研 harness"这一核心承诺，落在全站最薄的一层里，只有 2594 字符**。
- **P1 只把 prompt 和 pitfalls 拉平了，Track C 从未被碰过**——最短板被整体"挪"到了这里。

### 炮点 4 ·【内容注水】体量增长有相当比例来自模板复制

```
"## 小测验"       出现于 18 个文件
"三档 / 产物化三档" 出现于 11 个文件
"难度分层"         出现于 22 个文件
```

每页结尾的"小测验 + 三档表"高度同构。P1 的字符数增长中，**模板复制贡献显著**。用"字符数极差 ≤1.8"当验收口径，**恰好会被模板掩盖真实深度差异**——这是口径设计的问题。

### 炮点 5 ·【"源码级"成色不足】实例章平均只有 1–2 个真实代码块

| 实例章 | 字符 | 真实代码块 |
|---|---|---|
| deepagent | 5070 | **python ×1** |
| hermes | 3828 | python ×2 |
| openclaw | 3857 | yaml ×1 + ts ×2 |
| claude-code | 4002 | json ×2 + md ×1 |
| codex | 4384 | toml ×1 + py ×1 + md ×1 |
| deepseek-harness | 4804 | ts ×2 |

以 ~4300 字符覆盖一整个框架，**平均 1.5 个代码块**。宣称"源码级"，实际是"**一段示例 + 一张映射表 + 一张 mermaid**"。deepagent 更极端：5070 字符只有 1 个 python 块。

### 炮点 6 ·【可信度风险】新增"真实代码"是示意实现，且标注不一致

```
"示意" 标注次数：claude-code 0 · codex 0 · deepseek-harness 0   ← 未标注
                deepagent 1 · hermes 1 · openclaw 1
```

codex 的 `config.toml`、deepseek 的 TS 片段**未声明是"示意"还是原文**。读者无法区分哪些是**真实键名**（可照抄）、哪些是**拟构**（照抄会报错）。**越像真的、越危险**——被当成官方配置照抄是直接的生产事故。

### 炮点 7 ·【gate 页名不副实】4 个新建 gate 页不含脚本正文

```
python 代码块数：graph_gate 1 · context_gate 0 · harness_gate 0 · loop_gate 0 · skill_gate 0
```

4 个新 gate 页只给"运行命令 + 输出摘要"，**读者仍看不到脚本代码**。P0 的"让 gate 可见"**只做了一半**——从"看不到"变成"点得进但读不到"。

### 炮点 8 ·【可维护性】权威源引用无统一格式

有的用站点名（openclaw-docs）、有的用作者名（smartloli / iceyao）、有的用仓库名（deusyu/harness-engineering）——**无统一引用格式，无法机器校验覆盖度**（本次审查只能靠模糊 grep，正说明站点自身也无法自检）。

---

## 阶段二 · 支持方（客观成立条件，非吹捧）

1. **Mermaid 引用完整性 100%**：全站 `style X` 引用未定义节点数 **= 0**（脚本实测），渲染不会中断。
2. **5 个 gate 全部真实可运行**：graph（真 LangGraph）/ context（真 tiktoken）/ harness、skill（真实文件断言）/ loop（真控制流），**无 FakeMaker 式自证**——这是全站最硬的可验证资产。
3. **Track A 内容量已达标且分布合理**：mechanism 2826–6631、design 3807–4773、pitfalls 2309–2938，三层梯度清晰。
4. **结构与导航已一致**：顺序（mechanism→design→pitfalls）三处同步；22 页有难度分层；6 实例有精确映射表。
5. **口径诚实**：首页/README 明确声明"概念地图 + 权威源导航 + 部分可运行验证"，并列出 gate 验证边界（含 loop gate 不验 LLM 质量）。
6. **可复现**：58 页、构建 exit 0、内容标记全部核验。

---

## 阶段三 · 中立裁判：此刻如何修改

**总判**：v0.8 的问题**不是"薄"，而是"虚"**。下一步**停止增加新层与新标签**，把**引用体系与最薄一层补实**。判定优先级按"是否为一级缺陷（读者被误导）"排序。

### P0 —— 一级缺陷，必须先修（读者被引导到不存在的页面 / 无法核验来源）

1. **建真正的"事实源"页** `practice/sources.md`：逐条列出全部权威源（URL + 作者/站名 + 覆盖章节 + 是否逐字直读）。把全书 `详见 [事实源](/practice/compare)` 改指到此页。
2. **补齐 3 个未落地权威源**：loop 补 `cnblogs.com/xiaobaiysf/p/21964451`、deepseek 补 `ChesterXue` 篇、deepagent 补 `weixin_44733966` 篇——并在正文引用处给出**可点击链接**。

### P1 —— 让最短板与"名不副实"的资产达标

3. **Track C 扩容**：`build.md`（自研 harness）2594 → **≥5000**，补真实脚手架代码；`compare.md` 补每格评分的依据。
4. **4 个 gate 页内嵌脚本正文**（对齐 graph_gate），从"点得进"变为"读得到"。
5. **统一引用格式**：全文改为 `> 来源：[作者/站名](URL)（覆盖：x-1 / x-2）`，使覆盖度**可机器校验**。

### P2 —— 去水分、提纯度

6. **实例章去模板化并补代码**：削减同构的"小测验/三档"模板，把篇幅换成真实代码——**每个实例章 ≥3 个真实代码块**。
7. **代码可信度标注统一**：所有非逐字原文的代码，统一标 `【示意实现】`（当前 3 章未标）。
8. **升级验收口径**：废弃"字符数极差"，改用三项硬指标——
   - 每页 **真实代码块数**（非 mermaid）；
   - 每页 **可点击来源链接数**；
   - **去模板后的正文字符数**。

### 建议的新验收口径（可直接用）
| 指标 | 阈值 |
|---|---|
| 权威源落地率 | 用户给定权威源 **100% 出现在 sources 页并有 URL** |
| 死链 | `[事实源]` 类链接 **0 指向非来源页** |
| Track 体量比 | Track A : C **≤ 6 : 1**（当前 11.5 : 1） |
| 实例章真实代码块 | **每章 ≥3**（当前均值 1.5） |
| 引用格式 | **100% 带 URL** |

---

*本报告基于脚本实测（体量明细、代码块语言构成、Mermaid 引用检查、模板重复度、权威源 URL 全仓 grep）。承认 v0.8 在 Mermaid 完整性、gate 真实性、结构一致性上的客观成立；指出其"引用体系悬空 + 最薄层未补 + 增量靠模板"的实质问题。无主观吹捧。*